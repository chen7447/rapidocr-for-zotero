# 更新日志

## 2.1.0

- **修复双栏/小密字英文丢空格**：rec 模型（CTC）在双栏正文与小型字上经常不发空格类，复制/搜索整行粘连（如 `andTechnology`）。解码时按两条证据补回词间空格——词间空隙里的次强 Space 置信度（≥0.1，词内几乎恒零，分界面干净）与超长字符步长（宽字母 m/w 豁免，不会切出「fro m」）；只影响可打印 ASCII 邻对，中文不受影响。
- **减轻 OCR 进行中的界面顿挫**：每页渲染后原本无条件做一次 800 万像素级的诊断全扫描（主线程、多页任务每页都付这笔开销），改为稀疏采样的白页检测，完整统计只在调试开关打开时执行。

## 2.0.2

- **修复扫描件白屏**：图片型 PDF（CCITT/JBIG2 等，如专利扫描件）OCR 时过程图整页空白、`det=0`。pdf.js 6 对 `jar:`/`resource:` 解码资源走 `XMLHttpRequest`，而插件沙箱没有 XHR——现改为与模型一致的 `fetch()` 加载 wasm 解码器，扫描件能正常识别。

## 2.0.1

- **「去画选择区域」入口**：OCR 弹窗里新增按钮，一键切换到 Zotero 原生「选择区域」工具——很多用户不知道工具栏里那个虚线框图标就是圈选区域用的；画完回来点 OCR，圈内优先识别。
- **弹窗界面重排**：检测参数（分辨率/灵敏度/过滤/倾斜/核数）收进默认折叠的「高级设置」，主路径只剩页码、识别模式、双栏与圈选；弹窗随系统亮暗主题配色，数字参数附白话提示，主按钮显示目标页码。
- **弹窗视觉统一**：OCR / 删除 / 去画选择区域三个按钮统一居中、配扫描框与橡皮擦图标；复选框不再被拉宽挤出换行。
- **「OCR 过程图」运行期开关**：分阶段诊断图改由帮助菜单 switch control——勾选后区域 OCR 弹过程图，测试时不必再改代码注释；默认关闭，不影响正式版。

## 2.0.0

- **「选择区域」圈选修复**：此前标注坐标按页顶原点解释，所有框整体上下镜像、命中全错；现按 PDF 原生页底原点解析，框选命中精确。
- **圈选优先、圈外补足**：勾选「选择区域」后不再只认框内——整页检测一次，圈内行先输出；圈外行照常识别，接在后面。勾「双栏版面」时补足部分先左栏后右栏，未勾则按上下序。只圈摘要及以上、正文用「双栏版面」补足即可。
- **双栏版面阅读序**：标题、摘要等横贯带切区，区内先左栏后右栏，不再逐行左右交错。
- **高分辨率抢救**：识别未返回的小字行（邮箱、DOI、脚注等）自动按 4 倍分辨率重渲染重识别。
- **修复垃圾过滤误杀**：合法文本中的 `9999` 等重复串（如邮箱用户名）不再被当作识别伪影丢弃。
- **整行完整写入**（1.10.0）：被框盖住一半以上的整行完整输出，含跨框行，不再截断成半行。
- **诊断日志**：帮助菜单日志窗口含圈内、圈外、未返回分阶段计数与明细。
